<Page x:Class="StrayDogs.Pages.ProbaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StrayDogs.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="ProbaPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBox
             Width="200"
                  Height="200"
                  Grid.Column="3"
                  Grid.Row="5"
                  Grid.ColumnSpan="5"
                  Grid.RowSpan="2"
            />

        <ListView x:Name="Lv" 
                  Panel.ZIndex="4"
                  ItemsSource="{Binding }"
                  Width="400"
                  Height="400"
                  Grid.Column="1"
                  Grid.Row="2"
                  Grid.ColumnSpan="5"
                  Grid.RowSpan="2" d:ItemsSource="{d:SampleData ItemCount=2}"
            >
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

                <ListView.ItemTemplate>
                <DataTemplate>
                    <Border
                        BorderBrush="#16343C"
                        BorderThickness="1"
                        CornerRadius="5"
                        Padding="10"
                        >
                        <StackPanel Orientation="Vertical">
                            <Border
                             BorderBrush="#16343C" Opacity="0.8"
                             BorderThickness="1"
                             CornerRadius="5"
                             Padding="10"
                                >
                                <StackPanel>
                                    <TextBlock Text="Volier"/>
                                    <TextBlock Text="Descroption"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                </DataTemplate>                   
           </ListView.ItemTemplate>

        </ListView>
    </Grid>
</Page>
